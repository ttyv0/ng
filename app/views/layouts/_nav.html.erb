<nav class="navbar navbar-inverse navbar-static-top" role="navigation">
  <div class="container">
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to "Nothing Good", :posts, class: 'navbar-brand' %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="active">
          <%= link_to signed_in? ? current_user.name : "Anonimous", signed_in? ? edit_user_registration_path : new_user_session_path %>
        </li>
        <% if signed_in? %>
          <li>
            <a href="<%= render plain: destroy_user_session_path %>" rel="nofollow" data-method="delete">
              <span class="glyphicon glyphicon-log-out"></span>
            </a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
