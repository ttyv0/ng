<% if current_page.last? %>
  <li class="disabled">
    <span>
      <%= render plain: t('views.pagination.last').html_safe %>
    </span>
  </li>
<% else %>
  <li>
    <%= link_to t('views.pagination.last').html_safe, url %>
  </li>
<% end %>
